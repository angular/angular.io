a(class="hover-card is-button has-icon" href="#{url}" md-button)
  span(class="hover-card-icon #{classes}")
  if number
    h3 #{number}. #{name}

  else
    h3 #{name}

  if cta
    p #{cta}
  else
    p View #{name} Docs
