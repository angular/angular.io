'use strict'; // necessary for es6 output in node

import { browser
/*, element, by, ElementFinder*/
} from 'protractor';

describe('RxJS', function () {

  beforeAll(function () {
    browser.get('');
  });
});
